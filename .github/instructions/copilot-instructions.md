You are analyzing a Python repository that is already partially structured as a package.

Repository structure:

spinstep/ contains the core implementation
benchmark/, demos/, examples/ contain usage and experiments
tests/ contains unit tests
docs/ contains documentation
Packaging files (pyproject.toml, setup.py, etc.) already exist

Your task is to refactor this into a clean, production-quality Python library.

Work in stages. Stage-specific instructions are defined in .github/instructions/.

Always begin with Stage 1.
